import AuthenticationType from "@microsoft/powerplatform-cli-wrapper";

export default function getAuthenticationType(): AuthenticationType {
  const inputValue = task.getInput("authenticationType");
}
